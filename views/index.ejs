<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DB Session</title>
  </head>
  <body>
    <div class="container">
      <form action="/create-post" method="post">
        <label for="messageAuthor">Enter Your Name:</label>
        <input type="text" id="messageAuthor" name="messageAuthor" />
        <br />
        <br />
        <label for="messageText">Enter Your message:</label>
        <textarea
          name="messageText"
          id="messageText"
          cols="30"
          rows="10"
        ></textarea>
        <input type="submit" value="Post a message" />
      </form>
      <br /><br />
      <hr />
      <br /><br />
      <div class="results-field">
        <% for (let i = 0; i < posts.length; i++) { %>
        <div>
          <span
            >Posted By: <%=posts[i].messageAuthor %> -
            <span><%= posts[i].createdAt %></span></span
          >
          <p><%= posts[i].messageText %></p>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
